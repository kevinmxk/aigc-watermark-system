# AIGC 三级版权溯源系统 - 依赖配置说明
# ============================================================
# 由于三层架构依赖不兼容，需要创建三个独立的 conda 环境
#
# 环境1: daam (diffusers 0.21.2) - DAAM 语义分析
# 环境2: tree-ring (diffusers 0.11.1) - Tree-Ring 频域水印
# 环境3: stegastamp (TF 1.15.5) - StegaStamp 像素域水印
#
# 安装命令：
#   conda create -n daam python=3.8
#   conda activate daam
#   pip install -r requirements-daam.txt
#
#   conda create -n tree-ring python=3.8
#   conda activate tree-ring
#   pip install -r requirements-treering.txt
#
#   conda create -n stegastamp python=3.7
#   conda activate stegastamp
#   pip install -r requirements-stegastamp.txt
# ============================================================

# BCH 纠错码 (用户ID编码增强)
# bchlib>=1.0.0
# reedsolo>=1.5.4

# 基础依赖（所有环境共用）
torch>=2.0.0
torchvision>=0.15.0
Pillow>=9.0.0
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0
pyyaml>=6.0
tqdm>=4.65.0

# 其他依赖根据环境选择安装
